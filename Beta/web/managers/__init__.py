"""Quantum VRM — Python (PyScript) managers package.

Faithful Python ports of the original VRM_1 `/managers/*.js` modules, running in
the browser via Pyodide. Three.js / @pixiv/three-vrm remain the WebGL rendering
engine (exposed on `window`); all application logic here is Python.
"""
